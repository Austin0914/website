/** 
 * === ==
 * != !==
 * > < >= <=
 * || &&
 * Number() toString NaN
 * turthy vs falsy
 * false,0,"",null,undefined
 * array
 * index length push pop shift unshift
 * fuction parameter return
 */

function call(name){
    alert("Welcome "+name);
}

let input=prompt("Please Enter Your Name in English!");
call(input)
